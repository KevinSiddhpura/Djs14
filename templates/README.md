# Djs14 Templates

Use these starter files as quick scaffolds.

- chatInput.js: slash command template
- contextUser.js: user context menu command template
- contextMessage.js: message context menu command template

Copy any template into the matching folder under src/ and rename as needed.
